name: package_context
description: >
  Initialize a package's config and dependencies as one graph, and read them
  from a typed package context.
version: 2.0.0
repository: https://github.com/pchkauu/package_context
homepage: https://github.com/pchkauu/package_context
issue_tracker: https://github.com/pchkauu/package_context/issues
topics:
  - architecture

environment:
  sdk: ^3.13.2

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

dependencies:
  meta: ^1.16.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
