# package_context
A Dart package that helps define the context and boundaries of packages. It allows you to determine the correct direction of dependencies between packages and the application at compile time.
